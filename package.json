{
  "private": true,
  "scripts": {
    "create:db": "wrangler d1 create test-db",
    "dev": "wrangler d1 execute test-db --file=./schema.sql && wrangler dev --local",
    "login": "wrangler login",
    "build": "wrangler build",
    "publish": "wrangler publish"
  },
  "dependencies": {
    "wrangler": "^3.32.0"
  }
}
