{
  "private": true,
  "scripts": {
    "create:db": "wrangler d1 create test-db",
    "create:table": "wrangler d1 execute test-db --file=./schema.sql",
    "dev": "wrangler d1 execute test-db --file=./schema.sql --local && wrangler dev",
    "login": "wrangler login",
    "build": "wrangler build",
    "publish": "wrangler publish"
  },
  "dependencies": {
    "wrangler": "^3.32.0"
  }
}
